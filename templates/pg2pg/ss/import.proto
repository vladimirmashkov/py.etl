syntax = "proto3";

message table {
    // repeated StudentEducationGroupRow packet = 1;
    repeated tuple row = 1;
}

message tuple {
    int32 user_id = 1;
    int32 education_group_id = 2;
    string created_at = 3;
    string updated_at = 4;
}